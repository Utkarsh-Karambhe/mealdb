version: '3.8'
services:
  backend:
    build: ./mealdb-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod

  frontend:
    build: ./mealdb-app/mealdb-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
